[package]
name = "smop"
version = "0.2.0"
edition = "2024"
description = "Batteries-included scripting utilities for Rust"
license = "MIT OR Apache-2.0"
keywords = ["scripting", "cli", "utilities"]
categories = ["command-line-utilities", "filesystem"]
repository = "https://github.com/madmax983/smop"
homepage = "https://github.com/madmax983/smop"
documentation = "https://docs.rs/smop"
readme = "README.md"

[features]
default = ["full"]
full = ["http", "cli", "print", "csv", "time", "archive"]
http = ["dep:ureq"]
cli = ["dep:clap"]
print = ["dep:console", "dep:indicatif", "dep:dialoguer", "dep:comfy-table"]
csv = ["dep:csv"]
time = ["dep:chrono"]
archive = ["dep:zip", "dep:tar", "dep:flate2"]

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = { version = "3", features = ["json"], optional = true }
clap = { version = "4", features = ["derive"], optional = true }
console = { version = "0.15", optional = true }
indicatif = { version = "0.17", optional = true }
dialoguer = { version = "0.11", optional = true }
csv = { version = "1.3", optional = true }
comfy-table = { version = "7.2", optional = true }
chrono = { version = "0.4", optional = true }
zip = { version = "2.2", optional = true }
tar = { version = "0.4", optional = true }
flate2 = { version = "1.0", optional = true }
dirs = "6"
shellexpand = "3"
dotenvy = "0.15"
glob = "0.3"
toml = "0.8"
tempfile = "3"
which = "7.0"

[dev-dependencies]
mockito = "1"

[[example]]
name = "fetch_api"
required-features = ["http", "cli", "print"]

[[example]]
name = "file_processor"
required-features = ["print"]

[[example]]
name = "interactive_cli"
required-features = ["print"]

[lints.rust]
# Using deny (not forbid) to allow unsafe in tests for std::env::set_var (Rust 2024)
unsafe_code = "deny"

[lints.clippy]
all = "warn"
pedantic = "warn"
nursery = "warn"
unwrap_used = "warn"
expect_used = "warn"
